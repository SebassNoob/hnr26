{
  "name": "@hnr26/urmom",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:simple": "uv run src/main.py",
    "build": "uvx pyinstaller build.spec --clean",
    "dev": "uvx pyinstaller build.spec --clean --dev",
    "format": "uv format",
    "clean": "rm -rf dist/ build/ __pycache__/"
  }
}
