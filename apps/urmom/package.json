{
  "name": "@hnr26/urmom",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:simple": "uv run src/main.py",
    "build": "dev=0 uv run pyinstaller build.spec --clean",
    "dev": "dev=1 uv run pyinstaller build.spec --clean",
    "format": "uv format",
    "clean": "rm -rf dist/ build/ __pycache__/"
  }
}
